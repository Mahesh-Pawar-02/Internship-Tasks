import java.util.ArrayList;

public class Main {
    public static void main(String[] args) {
        ArrayList<Reservation> reservations = new ArrayList<>();

        // Start the login form
        LoginForm login = new LoginForm();

        // After successful login, the reservation form and cancellation form can be opened
        // This logic can be extended to open both reservation and cancellation windows
    }
}
